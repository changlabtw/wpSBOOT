# External tool binaries (install via setup.sh; see README)
bin/t_coffee
bin/raxml-ng

# Compiled wei_seqboot binary (built from src/ via setup.sh or make)
bin/wei_seqboot

# Keep the bin/ directory in the repo
!bin/.gitkeep

# Compiled object files and binaries
src/*.o

# Pipeline output directories
results/
test_output/

# Log files (includes wpsboot.log written to output directories)
*.log

# macOS
.DS_Store

# Editor
*.swp
*~
